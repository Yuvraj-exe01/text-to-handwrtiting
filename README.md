# text-to-handwrtiting
a full-stack web application that converts typed text into realistic handwritten notebook assignments using custom .ttf handwriting fonts. Developed the frontend with React.js and created a Node.js + Express backend integrated with a Python (Pillow) rendering engine for document generation. 
